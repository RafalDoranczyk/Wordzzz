import styled, { css } from "styled-components";

export const IconLabel = styled.label(
  ({ theme }) => css`
    display: flex;
    justify-content: center;
    align-items: center;
    width: 50px;
    height: 100%;
  `
);
